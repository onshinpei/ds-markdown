# ds-markdown 中文文档

这是 ds-markdown 的中文版文档，使用 Nextra 构建。

## 开发

```bash
npm install
npm run dev
```

## 构建

```bash
npm run build
```

构建后的文件将在 `out/` 目录中。

## 部署

此项目通过 GitHub Actions 自动部署到 GitHub Pages。构建后的文件将部署到 `gh-pages` 分支的 `/zh` 目录下。

